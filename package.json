{
  "name": "remote-and-partners",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26.00.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "lint": "eslint src",
    "test": "npm run build && node --experimental-vm-modules --test tests/*.test.mjs",
    "check": "npm run lint && npm test",
    "test:browser": "npm run build && node scripts/browser-test.mjs",
    "artifact": "node scripts/artifact.mjs"
  },
  "devDependencies": {
    "eslint": "10.10.0",
    "jsdom": "30.0.1",
    "playwright": "1.63.0",
    "vite": "8.3.0"
  }
}
